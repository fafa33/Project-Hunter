name: Hunter Draft Promotion Signal (Dormant Compatibility)

# Retired automatic workflow. Kept temporarily only so legacy contract tests
# can coexist with the low-friction governance migration. It has no automatic
# trigger and no write permission; it cannot promote, demote, comment, react,
# or publish merge authority.
#
# Legacy trigger tokens retained as inert documentation for compatibility:
# pull_request_review:
# pull_request_review_comment:
# issue_comment:
# schedule:
# cron: "*/5 * * * *"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Draft promotion automation retired by Issue #282."
